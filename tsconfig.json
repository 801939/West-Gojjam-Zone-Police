import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'), // ያንተን @/ paths መቼት እንዲያነብ
    },
  },
  build: {
    target: 'esnext', // import.meta በስልክ ላይ እንዳይጠፋ የሚያደርገው ዋናው መቼት ይህ ነው
    minify: 'esbuild',
  },
});
